import { createFileRoute } from '@tanstack/react-router'
import { ContactBay, PageHero, SectionLead, pageHead } from '../components/Site'
import { services, software } from '../content/site'
export const Route = createFileRoute('/services')({ head: () => pageHead('Engineering Services', 'Explore YUKTI capabilities across electrical, civil, structural and mechanical engineering, audits, design documentation and project support.'), component: Services })
function Services(){ return <main id="main"><PageHero index="02" title="Disciplined analysis. Practical delivery." intro="From focused technical studies to multidisciplinary project support, our services are shaped around the decision, risk and outcome at hand." />
<section className="service-index page-section"><SectionLead index="A" label="Expertise" title="Core disciplines and specialist support." />
<div className="service-visual-grid">{services.map(s=><a className="service-card" key={s.slug} href={`/services/${s.slug}`}><figure>{s.image && <img src={s.image} alt={s.imageAlt} loading="lazy" />}</figure><span>{s.index}</span><div><h2>{s.title}</h2><p>{s.short}</p></div><b>↗</b></a>)}</div></section>
<section className="software-section page-section"><SectionLead index="B" label="Technical environment" title="Established tools, directed by engineering judgement." text="Software supports the analysis. The quality of the outcome still depends on the assumptions, checks and decisions around it." light/><div className="software-list">{software.map((x,i)=><span key={x}>{String(i+1).padStart(2,'0')} / {x}</span>)}</div></section><ContactBay /></main> }
